name: oscar
description: "OSCAR is a state render with bloc."
version: 0.0.1
homepage: "https://github.com/mobinbyn/oscar"

environment:
  sdk: '>=3.3.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter


  # Core
  bloc: ^8.1.4
  flutter_bloc: ^8.1.5

  # Utility
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
